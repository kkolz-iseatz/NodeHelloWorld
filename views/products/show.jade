extends ../layout

block content
  #show
    h2=product.name
    p= product.description
    p Price: #{product.price}
    p
      a(href='/products/'+product.id+'/edit') Edit
      a(href='/products') Back
